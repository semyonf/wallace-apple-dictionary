import { WallaceWikiWebsite } from './wallace-wiki-website';

describe(WallaceWikiWebsite.name, () => {
  it('should be defined', function () {
    expect(WallaceWikiWebsite).toBeDefined();
  });
});
